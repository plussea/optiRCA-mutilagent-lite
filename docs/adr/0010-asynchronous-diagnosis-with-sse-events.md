# 异步诊断运行与 SSE 事件流

前端正式诊断采用异步 session：创建诊断运行后立即返回 session 标识，浏览器通过 SSE 接收阶段开始、阶段产物、阶段完成、失败与最终结论。相同事件同时写入 SQLite，既支持完成后原样回放，也支持浏览器断线后从最后事件继续；取消操作通过独立接口停止后续 Agent 调度。

选择 SSE 是因为诊断进度是单向、连续的服务端事件流，且仓库已有播放事件契约与事件存储。现有同步 `POST /api/v1/diagnose` 在迁移期保留兼容，新的工作台使用异步运行接口；只有运行完成或降级结束才生成正式诊断案卷，用户取消不生成案卷。
