# 拓扑预检作为正式诊断硬门禁

当诊断样本未提供拓扑 JSON 时，系统先执行拓扑预检，并只在以下情形允许进入正式诊断：相关告警明确收敛于同一设备，或告警中的端口与定位信息能够唯一还原链路两端。多设备连接不唯一、端口存在多个可能远端、链路只有不可解释的单端信息、标识缺失或矛盾，以及需要判断分支、环路或多条传播路径但无法从 CSV 还原时，必须要求补充拓扑 JSON，不允许用户绕过。

该决策选择可审计性与诊断可信度，而不是最大化无拓扑场景覆盖率。预检失败属于“输入未就绪”，不启动 Agent、不生成诊断案卷，也不计入诊断质量统计；预检通过后发生的失败才属于“降级诊断”。
