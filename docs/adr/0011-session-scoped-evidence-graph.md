# 异构证据图按诊断运行隔离

共享黑板的“共享”仅指同一诊断运行内各 Agent 共享异构证据图，不允许不同 session 读写同一份可变图状态。每个正式诊断运行必须拥有独立的证据图命名空间或快照，完成后的图随诊断案卷持久化；新运行不得继承上一运行的节点与边。

该决策保留 ADR-0001 的 Agent 协作模式，同时消除跨请求节点冲突、结果污染与并发覆盖。Demo 的最低验收是同一页面连续运行三次，每次创建独立 session 和案卷并稳定得到 `link:N1-N2`；页面刷新可按 session 恢复当前运行或最终结果。
