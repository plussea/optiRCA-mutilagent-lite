# 首版诊断限定为单根因场景

首版工作台与诊断契约只承诺单根因场景，当前 Demo 和首批测试样本均按单一可修复物理根因验收。当 Critic 识别出多个相互独立且无法由同一故障解释的告警簇时，系统不得强行选择一个 Top-1，而应输出“疑似多故障场景”的降级诊断，保留各告警簇和候选根因作为人工复核线索。

该边界保持现有单一 `root_cause`、诊断结论卡和案卷模型的一致性，也优先保证评审可信度。真正的多根因支持需要独立扩展输出 Schema、排序逻辑、Critic 规则、业务拓扑表达与评测指标，不能以界面拼接多个候选替代。
